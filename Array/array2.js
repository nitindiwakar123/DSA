// WAP to swap two index values of the array.
function swap(arr, a, b) {
    const temp = arr[a];
    arr[a] = arr[b];
    arr[b] = temp;

    console.log(arr);
    
}

// swap([1, 2, 3, 4, 5, 6], 1, 4);

// 